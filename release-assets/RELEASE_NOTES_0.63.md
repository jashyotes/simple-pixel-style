# Simple Pixel Style 0.63

- Refines the Your Day overlay current-hour marker so the current hour is shown by the pipe/elbow indicator instead of also drawing a circle pip.
- Changes the rolling Your Day overlay title from the hour-count window label to `Future`.
- Leaves the restored v0.62 color mode, rolling/fixed window settings, and Step Graph removal intact.
