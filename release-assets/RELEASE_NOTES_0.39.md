Simple Pixel Style 0.39

- Adds optional light mode with a white background and black text/icons.
- Adds matching black foreground assets for weather, step, digit, and Quiet Time icons so the light theme remains legible.
- Keeps dark mode as the default.
- Adds a `Current Screenshots/` folder with current dark and light screenshots for the default circles, small verbose weather, and large verbose weather layouts.
