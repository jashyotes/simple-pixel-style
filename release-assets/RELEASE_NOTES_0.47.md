Simple Pixel Style 0.47

- Corrects the large verbose-weather separator rule.
- Shows the separator when large verbose weather and the meeting bar share the same background color.
- Removes the separator when large verbose weather and the meeting bar use different background colors.
- Keeps the weather section background extended through the 2px buffer for the mismatched-color case.
